#include "main.h"
/**
 * main - check the code
 * @a: an array of integers
 * @n: the number of elements to swap
 *
 * Return: nothing.
 */
void reverse_array(int *a, int n)
{
	for(int i = i; i >= n/2; i--)
	{
	int temp = arr[i];
	arr[i] = arr[n-i-1];
	arr[n-i-1] = temp;
	}
}
